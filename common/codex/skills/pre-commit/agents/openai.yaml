interface:
  display_name: "Pre-commit"
  short_description: "Review and validate changes without committing"
  default_prompt: "Use $pre-commit to review my current changes, run checks, fix issues, and provide a detailed pre-commit summary."
